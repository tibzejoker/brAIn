{
  "name": "mcp-host",
  "description": "Bridges external MCP servers onto the brAIn bus. Supports four transports: stdio (local subprocess), http (modern Streamable HTTP — for cloud-hosted servers like Linear, Notion), sse (legacy HTTP/SSE), ws (WebSocket). Tools become callable via mcp.call ({server?, tool, arguments?}). The brain calls them like any other node.",
  "tags": ["service", "mcp", "tools"],
  "default_authority": 1,
  "default_priority": 3,
  "default_subscriptions": [
    { "topic": "mcp.call" },
    { "topic": "mcp.tools.list" }
  ],
  "default_publishes": ["mcp.result", "mcp.tools.available"],
  "supports_transport": ["process"]
}
