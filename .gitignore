node_modules/
dist/
data/

# nats-server binary downloaded by scripts/install-nats.mjs
packages/core/bin/
.env
*.log
.DS_Store
nodes/_dynamic/
!nodes/_dynamic/.gitkeep
/.prompt.tmp
.claude/

# Python venvs + caches under any node / SDK
.venv/
__pycache__/
*.pyc
*.pyo
/.test-fresh-install/

# Vite emits compiled .js next to vite.config.ts when its tsconfig
# accidentally includes the file — and vite then prefers the stale
# compiled artefact over the live .ts. Never commit them.
packages/dashboard/vite.config.js
packages/dashboard/vite.config.js.map
packages/dashboard/vite.config.d.ts
