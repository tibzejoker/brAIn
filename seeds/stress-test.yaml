nodes:
  - type: clock
    name: fast-clock
    priority: 1

  - type: clock
    name: slow-clock
    priority: 1

  - type: echo
    name: echo-all
    subscriptions:
      - topic: "time.*"
      - topic: "echo.*"
        mailbox:
          max_size: 50
          retention: lowest_priority
